version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm install -g cross-env
    build:
      commands:
        - node -v
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/* 